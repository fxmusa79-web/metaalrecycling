# Frontend / Pages environment (no secrets in the browser bundle)

# Public Turnstile site key (optional, when anti-spam is enabled)
VITE_TURNSTILE_SITE_KEY=

# Worker API base URL (production example)
# VITE_API_BASE=https://duurzaammetaalrecycling.nl
