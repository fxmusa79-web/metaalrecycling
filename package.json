{
  "name": "duurzaammetaalrecycling",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "prebuild": "node scripts/write-build-meta.mjs",
    "dev": "vite",
    "dev:api": "wrangler dev --port 8787",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy",
    "cf:whoami": "wrangler whoami"
  },
  "devDependencies": {
    "vite": "^8.3.0",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "gsap": "^3.15.0"
  }
}
