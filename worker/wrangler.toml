# API-only config kept for local `cd worker && wrangler dev`.
# Production deploy uses the root wrangler.toml (static assets + /api/*).

name = "duurzaammetaalrecycling-api"
main = "src/index.js"
compatibility_date = "2025-09-01"

[vars]
CONTACT_EMAIL = "info@duurzaammetaalrecycling.nl"

# Secrets (set via `wrangler secret put`, never commit):
# RESEND_API_KEY
# TURNSTILE_SECRET_KEY
